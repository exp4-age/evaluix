Installation
============

Install from PyPI::

    pip install evaluix

For development (editable install)::

    git clone https://github.com/exp4-age/evaluix.git
    cd evaluix
    pip install -e .
